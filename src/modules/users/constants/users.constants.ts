/** P0 — a master may change their WhatsApp number at most once per 24 hours. */
export const WHATSAPP_CHANGE_COOLDOWN_MS = 24 * 60 * 60 * 1000;

/** B-50 — a client may keep at most this many live saved addresses. */
export const MAX_SAVED_ADDRESSES = 10;
